importar  'dardo: io' ;

vazio  principal ()
{
  altura dupla, base, área;
  
  print ( "Digite uma altura para o triângulo:" );
  
  altura =  .parse duplo (stdin.readLineSync ());
  
  print ( "Digite uma base para o triânguçp:" );
  
  base =  .parse duplo (stdin.readLineSync ());
  
  área = (base * altura) /  2 ;
  
  print ( "Área é = $ área " );
}